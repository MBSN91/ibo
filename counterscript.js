var waypoint = new Waypoint({
    element: document.getElementById('stoffen'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('stoffen', 7068, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })

  var waypoint = new Waypoint({
    element: document.getElementById('voering'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('voering', 168, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })  

  var waypoint = new Waypoint({
    element: document.getElementById('knoop'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('knoop', 81, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })
  
  var waypoint = new Waypoint({
    element: document.getElementById('kraag'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('kraag', 32, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })
  


  var waypoint = new Waypoint({
    element: document.getElementById('garen'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('garen', 22, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })
  
  var waypoint = new Waypoint({
    element: document.getElementById('rever'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('rever', 23, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })

  var waypoint = new Waypoint({
    element: document.getElementById('zakken'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('zakken', 21, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })

  var waypoint = new Waypoint({
    element: document.getElementById('broek'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('broek', 16, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })

  var waypoint = new Waypoint({
    element: document.getElementById('gilet'),
    handler: function(direction) {
      const options = {
    duration: 5,
  };
  let demo = new CountUp('gilet', 6, options);
  if (!demo.error) {
    demo.start();
  } else {
    console.error(demo.error);
  }
    },
    offset: '100%'
  })
  
